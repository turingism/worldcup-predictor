# Repository summary: World Cup prediction analytics support file.
# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# 训练缓存（可由 get_model 重建）
model.pkl
model_meta.json

# 世界杯冻结账本与赛果**入库**（2026-07-26 起，原先按「运行时状态」排除）。
# 理由：账本是本项目最核心的证据——每条带 frozen_at、开球前写死、事后只读。
# 排除它会让 GitHub Pages 的 Demo 从干净克隆重建整份账本，104 条真冻结退化成
# 98 条全回溯（retro_n=98）且缺决赛，「赛前冻结、事后对账」这个最有力的主张
# 在访客眼里就不成立了。live_results.json 同理：committed 的 results.csv 停在
# 07-12，末段淘汰赛赛果只在它里面。
#   代价：本地跑 app 会改写这两个文件、产生 git 改动，提交前留意 diff 是否预期。
#
# 各赛事冻结账本（尚未产生；到期同样应考虑入库）。gitignore 不支持行内注释，故独立成行
data/predictions_*.json
data/overrides.json
data/xuanxue_ledger.json
data/jc_review.json
*.tmp

# 实验性 / 临时
_seed_live_demo.py

# bayes 生成产物（均可由 bayes.py 固定种子=42 / champ_ci.py 从 results.csv 重生成，
# 跟随当前数据每次刷新自动重写，非快照→不进 git，与下方 .npz 口径统一）
data/bayes_draws.npz
data/bayes_ratings.json
data/champ_ci.json

# ESPN 赔率快照（随赛事累积，本地运行时数据）
data/odds_snapshots.jsonl
data/handicap_lines.json
data/handicap_snapshots.jsonl

# Fjelstul World Cup DB 外部缓存（bt_knockout.py 首次运行自动下载）
data/fjelstul_matches.csv
data/fjelstul_goals.csv
data/club/
data/logs/

# golden diff 快照（scripts/golden_diff.sh capture 产物，本地验收用，脚本本身入库）
data/golden/

# 静态导出产物（由 GitHub Actions 生成并直接发布，不进 git）
dist/

# baoyu-translate refined-mode run artifacts（项目偏好配置保留，逐次草稿不入库）
.baoyu-skills/baoyu-translate/runs/
