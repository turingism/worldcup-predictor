# Repository summary: World Cup prediction analytics support file.
# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# 训练缓存（可由 get_model 重建）
model.pkl

# 运行时可变状态（app 每次运行会改写；非源码）
data/predictions.json
data/live_results.json
data/overrides.json
data/xuanxue_ledger.json
data/jc_review.json
*.tmp

# 实验性 / 临时
_seed_live_demo.py

# bayes 生成产物（均可由 bayes.py 固定种子=42 / champ_ci.py 从 results.csv 重生成，
# 跟随当前数据每次刷新自动重写，非快照→不进 git，与下方 .npz 口径统一）
data/bayes_draws.npz
data/bayes_ratings.json
data/champ_ci.json

# ESPN 赔率快照（随赛事累积，本地运行时数据）
data/odds_snapshots.jsonl
data/handicap_lines.json
data/handicap_snapshots.jsonl

# Fjelstul World Cup DB 外部缓存（bt_knockout.py 首次运行自动下载）
data/fjelstul_matches.csv
data/fjelstul_goals.csv
