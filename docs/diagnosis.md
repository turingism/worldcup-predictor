# 联赛拓展失败归因诊断报告

日期：2026-07-19 17:55（北京时间）
诊断方式：git 历史核查 + 链路逐层实测（每层用实际运行结果证明，无推测）

## 一、总结论

「此前多次联赛拓展均未成功、前端始终没有联赛界面」的归因**不是故障，而是计划内的分阶段施工**：

- P0（离线层）已全部建成且实测可用：数据装载、每联赛模型、赛季模拟器、赛事注册表、144 俱乐部中文映射。
- L3 API 层与 L4 前端层**刻意零接线**——项目纪律规定「世界杯运行期（至 2026-07-19 决赛）app 冻结大改」，接线工作（P1）定于决赛后按 `docs/P1_WIRING_CHECKLIST.md` 施工。
- 不存在「代码写了但没被调用」的孤儿路由或「路由注册了但前端没入口」的断头路：接线代码根本尚未编写，这是有意为之并有完整书面记录（CLAUDE.md 自 2026-07-08 起十余条接手记录、MULTI_EVENT_PLAN.md 路线图）。

## 二、git 与代码痕迹核查

1. `git status`：`clubdata.py`、`clubpredict.py`、`clubsim.py`、`events.py`、`bt_club_hl.py`、`bt_club_market.py` 均为**未跟踪新文件**，从未有任何 commit 将其接入 `app.py`。
2. `grep clubdata|clubsim|events app.py`：**零命中**——app 后端不 import 任何俱乐部/多赛事模块。
3. `git log`：近 20 条 commit 全部为世界杯主线（竞彩复盘、口径勘误、评审落地），无联赛接线尝试的回滚或残骸。
4. CLAUDE.md 各期接手记录反复出现「纯离线旁路、运行中 app 零接线」「P1（7-19 决赛后动工）」字样——冻结是明文纪律，不是遗忘。

## 三、链路逐层实测（2026-07-19 当日）

| 层 | 结论 | 实测证据 |
|---|---|---|
| 数据源可达性 | 通 | football-data.co.uk 数据已本地缓存 |
| 数据入库 | 通 | `data/club/` 下 E0/SP1/I1/D1/F1 五大联赛 + E1/SP2/I2/D2/F2 feeder 各约 7 季 CSV 齐全（E0 近 7 季 2660 场，截至 2025-05-25，25-26 季 8 月开赛后需 `_CUR_END+1` 并 `--refresh`） |
| 预测计算 | 通 | `python3 clubpredict.py "阿森纳" "曼城"` → 39.7/26.4/33.9，xG 1.40-1.28，Top7 比分正常输出（模型缓存 `data/club/model_E0.pkl` 命中） |
| API 返回 | 不通（未实现） | `app.py` 共 30 条路由，零 club/events 引用；实测 `GET /api/dashboard?event=epl2526` 返回 200 但参数被忽略，返回的仍是世界杯数据 |
| 前端渲染 | 不通（未实现） | `templates/index.html` 无联赛 tab、无任何联赛入口（grep「英超」零命中） |

**断点定位：L3（API 层）与 L4（前端层），性质为计划内未实施。** 数据与算法两层无需修复。

## 四、修复方案与时点

- 施工蓝本已备齐：`docs/P1_WIRING_CHECKLIST.md`（P1 五步：event 上下文 → live/espn 参数化 → 账本隔离 → L0 切换器 → nl2026 壳，每步带行号锚点与独立验收标准）+ `docs/ia-mockup.html`（UI 蓝图）。
- 动工前置条件（清单明文）：① 决赛赛果回补完成；② 104 项基线测试全绿。
- 当前状态：决赛（西班牙 vs 阿根廷，北京时间 7-20 凌晨开球）尚未进行，冻结仍生效。本报告完成阶段 0 的诊断部分；「让任意联赛比赛出现在界面上」的接线证明将在解冻后作为 P1-④（L0 切换器）的第一个验收件交付，截图存 `docs/evidence/`。

## 五、对 /loop 任务书的两点勘误

1. 任务书阶段 1「数据源选型（football-data.org / API-Football / openfootball）」已被既有裁决覆盖：俱乐部数据源已选定 football-data.co.uk（B365 开盘+闭盘原生齐全、五联赛各约 3 季实测通过），国家队用 martj42；重新选型属重做已裁决事项。
2. 任务书阶段 2「Dixon-Coles 基线 + 联赛差异化参数」已完成并超额：每联赛独立模型、half_life=365 经 5 联赛 × 3 cutoff 正式复扫裁决（国家队 730 不可照搬）、市场对标基线 `bt_club_market.py` 已跑通（闭盘在全部 5 联赛胜模型，RPS gap 约 +0.010）。剩余主要工作在阶段 3（版式/接线）。
